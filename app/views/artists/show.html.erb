<h1>One Artist Information: <%= @artist.name %></h1>
<%= image_tag @artist.image_path %>
<p><%= link_to("Edit", edit_artist_path) %></p>
<p><%= link_to("Delete", edit_artist_path(@artist), method: :delete, data: {confirm: "Really delete the artist?"}) %></p>
